# Automatically load the Nix development environment
# Install direnv: https://direnv.net/
#
# The flake automatically activates the pixi environment if pixi.toml exists
# This exposes mojo, mojo-lsp-server, and other tools to PATH
use flake

# Watch pixi.toml for changes
watch_file pixi.toml
